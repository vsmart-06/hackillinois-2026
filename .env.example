# Optional: Modal endpoints (paste URLs after `modal deploy modal/modal_whisper.py` and `modal deploy modal/modal_inference.py`)
# MODAL_WHISPER_URL=https://your-workspace--semflow-whisper-transcribe-endpoint.modal.run
# MODAL_INFERENCE_URL=https://your-workspace--semflow-inference-recommend-endpoint.modal.run
